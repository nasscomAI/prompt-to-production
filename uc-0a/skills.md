# Skills

## classify_complaint
Takes one complaint description as input and returns:
- category
- priority
- reason
- flag

## batch_classify
Reads the input CSV file, applies classify_complaint to every row,
and writes the classified output to a new CSV file.


