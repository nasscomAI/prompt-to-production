# Skills

## classify_complaint

Input:
One complaint description.

Output:
- category
- priority
- reason
- flag

This skill analyzes the complaint text and assigns the correct category and priority.

## batch_classify

Input:
CSV file.

Output:
CSV file with category, priority, reason, and flag columns.

This skill reads all complaints and processes them one by one.