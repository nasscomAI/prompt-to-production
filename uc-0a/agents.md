# Complaint Classifier Agents

- `ClassifierAgent`: An agent specializing in citizen complaint categorization and priority assessment. It follows a strict set of categorization rules to ensure data consistency and urgency handling.
- `QualityControlAgent`: (Implied) Acts as the `NEEDS_REVIEW` flagger for ambiguous descriptions.
