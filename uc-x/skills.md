# Skills for UC-X
- `retrieve_documents`: Loads all 3 policy files, indexes by document name and section number.
- `answer_question`: Searches indexed documents, returns single-source answer + citation OR refusal template. Enforces no cross-document blending and no hedged hallucination.
