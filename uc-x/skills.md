skills:
  - name: retrieve_documents
    description: Loads all 3 policy files and indexes content by document name and section number.
    input: "No user question required; reads fixed files: policy_hr_leave.txt, policy_it_acceptable_use.txt, policy_finance_reimbursement.txt."
    output: "Structured index keyed by {document_name, section_number} with section text spans."
    error_handling: "If any source file is missing, unreadable, or unparseable, return a retrieval error listing exact file names and do not infer missing content."

  - name: answer_question
    description: Searches indexed policies and returns either a single-source cited answer or the exact refusal template.
    input: "Natural-language user question string plus the index generated by retrieve_documents."
    output: "Either (1) answer grounded in one document only with citation(s) as document name + section number for every factual claim, or (2) exact refusal template."
    error_handling: "If the question is not covered, ambiguous, or would require blending across documents, return the refusal template exactly and avoid hedging language."
